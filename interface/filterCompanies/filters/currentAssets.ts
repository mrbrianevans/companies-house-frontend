// FILE GENERATED BY generator.js
import { IFilterOption, INumberFilter, INumberFilterOption } from '../../../types/IFilters'
import { IMinorQuery } from '../../../types/IQueries'

export const filterCompaniesByCurrentAssets: (filter: INumberFilter) => IMinorQuery = (filter) => {
  const query = `
      SELECT company_number
      FROM wide_accounts_combined
      WHERE current_assets BETWEEN ? AND ?
  `

  return { query, value: [filter.min, filter.max] }
}

export const filterCompaniesByCurrentAssetsMetadata: INumberFilterOption = {
  category: 'current assets',
  possibleComparisons: ['is between'],
  valueType: 'number'
}
