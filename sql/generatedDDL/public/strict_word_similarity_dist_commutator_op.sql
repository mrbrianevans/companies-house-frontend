create function public.strict_word_similarity_dist_commutator_op(text, text) returns real
	immutable
	strict
	parallel safe
	language c
as $$
begin
-- missing source code
end;
$$;

alter function public.strict_word_similarity_dist_commutator_op(text, text) owner to black;

